services:
  - type: web
    name: personal-python-site
    runtime: python
    buildCommand: npm install && npm run build:css && pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
