[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements.txt && rm -f package-lock.json && npm install && npm run deploy"

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
