{
  "name": "personalpythonsite",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "uv sync && npm install",
    "build:css": "npx @tailwindcss/cli -i static/input.css -o static/output.css",
    "deploy": "npx @tailwindcss/cli -i static/input.css -o static/output.css --minify",
    "watch:css": "npx @tailwindcss/cli -i static/input.css -o static/output.css --watch",
    "dev": "concurrently --kill-others-on-fail -n css,server,mypy -c cyan,green,magenta \"npm run watch:css\" \"uv run uvicorn main:app --reload --reload-include '*.pyx'\" \"npm run watch:mypy\"",
    "type-check": "uv run python scripts/mypy_pyx.py",
    "watch:mypy": "uv run python scripts/mypy_pyx.py && uv run python -m watchfiles --filter python 'uv run python scripts/mypy_pyx.py' components/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nathanphoffman/PersonalPythonSite.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/nathanphoffman/PersonalPythonSite/issues"
  },
  "homepage": "https://github.com/nathanphoffman/PersonalPythonSite#readme",
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "concurrently": "^10.0.0",
    "tailwindcss": "^4.3.0"
  }
}
